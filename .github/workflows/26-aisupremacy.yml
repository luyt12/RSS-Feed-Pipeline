name: AI Supremacy RSS
on:
  schedule:
      - cron: '08 03 * * *'  # 北京时间 05:00
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install feedparser requests trafilatura beautifulsoup4 lxml
      - name: Process feed
        env:
          FEED_NAME: 'AI Supremacy'
          FEED_URL: 'https://www.ai-supremacy.com/feed'
          FEED_LANG: 'en'
          MAX_DAILY: 5
          KIMI_API_KEY: ${{ secrets.KIMI_API_KEY }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
        run: python daily_task_single.py
      - name: Commit processed URLs
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git pull --rebase || true
          git add data/processed_urls.json
          git commit -m "chore: update processed URLs for AI Supremacy" || true
          git push || true

